import styled from 'styled-components';

export const SoundButton = styled.button`
  position: absolute;
  color: white;
  background: none;
  border: none;

  height: 5em;
  width: 5em;
  svg {
    fill: currentColor;
  }

  right: 1em;
  bottom: 1em;
`;
